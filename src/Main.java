import view.MainFrame;

/**
 * Classe principale de l'application qui lance l'interface utilisateur.
 */
public class Main {

    /**
     * Méthode principale de l'application.
     * Crée une instance de MainPanel pour lancer l'interface utilisateur.
     * @param args Arguments de la ligne de commande (non utilisés dans cette application).
     */
    public static void main(String[] args) {
        new MainFrame();
    }
}